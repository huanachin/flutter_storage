name: flutter_storage
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.5
  provider: 6.0.4
  get_it: 7.2.0
  sqflite: 2.2.0+2
  injectable: ^1.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  injectable_generator: ^1.5.4
  build_runner: ^2.3.2

flutter:

  uses-material-design: true

